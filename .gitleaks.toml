title = "gitleaks config for ReneeBuildsTech.github.io"

[extend]
useDefault = true

[allowlist]
description = "Known false positives on static site copy"
fingerprints = [
  "index.html:generic-api-key:129",
]
